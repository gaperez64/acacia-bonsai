# Closing, 17s cap: admitted candidate = B (baseline)

No candidate was admitted this sprint (`../README.md` has the full disposition), so the closing
17s report is B (baseline) and ltlsynt only, exactly as they already appear in the opening report —
no new campaign, no new plot: reusing collected data per plan section 12.2 rather than re-running it.

- Full B/S/ltlsynt PAR-2 table and cactus plot (both epochs): `../../opening/17s/epoch-1/three-way-par2.md`,
  `.png`, `.pdf` (epoch 1, primary), and `../../opening/17s/epoch-2/` (epoch 2, repeatability).
- B's raw per-instance rows: `../../opening/17s/epoch-1/B-cap17-epoch1.csv` and the epoch-2 equivalent.
- ltlsynt's raw per-instance rows (one series, spread across the same shard protocol, not per-epoch):
  `../../opening/17s/ltlsynt-cap17.csv`.
- S appears in the same plots/tables, clearly labelled — see `../evaluated-not-admitted/README.md`
  for why it is shown but not the admitted candidate.
