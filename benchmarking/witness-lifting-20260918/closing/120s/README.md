# Closing, 120s cap: admitted candidate = B (baseline)

No candidate was admitted this sprint (`../README.md` has the full disposition), so the closing
120s report is B (baseline) and ltlsynt only, exactly as they already appear in the opening report —
no new campaign, no new plot: reusing collected data per plan section 12.2 rather than re-running it.

- Full B/S/ltlsynt PAR-2 table and cactus plot (both epochs): `../../opening/120s/epoch-1/three-way-par2.md`,
  `.png`, `.pdf` (epoch 1, primary), and `../../opening/120s/epoch-2/` (epoch 2, repeatability).
- B's raw per-instance rows: `../../opening/120s/epoch-1/B-cap120-epoch1.csv` and the epoch-2 equivalent.
- ltlsynt's raw per-instance rows: `../../opening/120s/ltlsynt-cap120.csv`.
- S appears in the same plots/tables, clearly labelled — see `../evaluated-not-admitted/README.md`
  for why it is shown but not the admitted candidate.
