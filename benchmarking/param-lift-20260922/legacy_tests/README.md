These four archived pytest files asserted the withdrawn production capability
route, including its switches and wrapper behavior. They are kept for audit,
with `.archive` extensions so current test discovery does not treat the
withdrawn behavior as a required production contract. The dated standalone
lifting tests remain in the parent directory.
