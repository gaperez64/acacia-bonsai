Transition system {
    State 0, initial:
        Outgoing transitions:
            to state 1 labeled ((!a1 && !a2) U (r1 && r2))
            to state 2 labeled ((!a1 && !a2) U (!r1 && r2))
            to state 3 labeled ((!a1 && !a2) U (r1 && !r2))
            to state 4 labeled ((!a1 && !a2) U (!r1 && !r2))
    State 4:
        Outgoing transitions:
            to state 1 labeled ((!a1 && !a2) U (r1 && r2))
            to state 2 labeled ((!a1 && !a2) U (!r1 && r2))
            to state 3 labeled ((!a1 && !a2) U (r1 && !r2))
            to state 4 labeled ((!a1 && !a2) U (!r1 && !r2))
    State 3:
        Outgoing transitions:
            to state 1 labeled ((a1 && !a2) U (r2))
            to state 4 labeled ((a1 && !a2) U (!r2))
    State 5:
        Outgoing transitions:
            to state 1 labeled ((a1 && !a2) U (r2))
            to state 4 labeled ((a1 && !a2) U (!r2))
    State 2:
        Outgoing transitions:
            to state 3 labeled ((!a1 && a2) U (r1))
            to state 4 labeled ((!a1 && a2) U (!r1))
    State 1:
        Outgoing transitions:
            to state 5 labeled ((!a1 && a2) U (T))
}
