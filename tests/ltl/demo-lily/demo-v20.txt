Transition system {
    State 0, initial:
        Outgoing transitions:
            to state 0 labeled ((sts && sflr && shlr) U (etc))
            to state 1 labeled ((sts && sflr && shlr) U (!etc))
    State 1:
        Outgoing transitions:
            to state 2 labeled ((sts && !sflr && !shlr) U (ec && etc))
            to state 3 labeled ((sts && !sflr && !shlr) U (!ec && etc))
            to state 0 labeled ((sts && !sflr && !shlr) U (!etc))
    State 3:
        Outgoing transitions:
            to state 2 labeled ((sts && sflr && !shlr) U (ec && etc))
            to state 3 labeled ((sts && sflr && !shlr) U (!ec && etc))
            to state 0 labeled ((sts && sflr && !shlr) U (!etc))
    State 2:
        Outgoing transitions:
            to state 3 labeled ((sts && !sflr && shlr) U (etc))
            to state 0 labeled ((sts && !sflr && shlr) U (!etc))
}
