Transition system {
    State 0, initial:
        Outgoing transitions:
            to state 1 labeled ((!fl && hl) U (ets))
            to state 0 labeled ((!fl && hl) U (!ets))
    State 1:
        Outgoing transitions:
            to state 2 labeled ((fl && !hl) U (T))
    State 2:
        Outgoing transitions:
            to state 3 labeled ((fl && !hl) U (ets))
            to state 2 labeled ((fl && !hl) U (!ets))
    State 3:
        Outgoing transitions:
            to state 0 labeled ((!fl && hl) U (T))
}
