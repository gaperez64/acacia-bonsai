# VERDICT — Stage C

**Implemented and verified on the frozen tlsf-tools 8b158d7 build after the Stage C review fixes.** The two stale strict xfails are removed in patch 00. Parametric TLSF enters a generic, source-bound online lift attempt; a nonparametric input uses Stage A's direct exact route. Source bytes, frontend provenance, structural seed windows, per-predicate exact BDD reconstruction, candidate export, policy/region checks, wrapper routing, and evidence are in patches 01–06; review fixes are in patch 07. No production decision uses a benchmark family, filename, historical table, or user signal spelling.

The target checker is the decision authority. A lifted answer is REALIZABLE only after `tlsfcertcheck` independently verifies the exact or REAL-sound strict target game and the bound candidate. The policy is Skolemized from the lifted invariant/ranks and exact target transitions by default. Optional learned move schemas are globally disabled; an enabled failed attempt declines. Evidence records the move source. Direct UNREALIZABLE remains an exact-route result. Unproved origin, role, bus, axis, rank, arity, move, or proof cases decline and reach direct solving or fallback.

Validation: the final serial full `tests/pytest` run passed **996**, skipped **2**, with 8 passing subtests using `ACACIA_TLSF_TOOLS_BUILD=/home/gperez/GIT-repos/tlsf-tools/build-SB-8b158d7`. It includes the checker mutation and review regression tests. `ruff check` and `git diff --check` passed. Patch 07 passed `git apply --check` after replaying 00–06 in a temporary tree. Nothing was staged or committed.

The full pytest run used `/usr/bin/python3.13` with `PYTHONPATH=/home/gperez/.cache/uv/archive-v0/NXo7adONRD1Kx-zpEow9x:/usr/local/lib64/python3.13/site-packages:virtenv/lib/python3.13/site-packages:benchmarking` so the installed NumPy, Spot, Acacia binding, and benchmarking helpers resolved together.

Informal serial 20+20 sample from the pre-review code, 60 s outer cap, fixed UNKNOWN fallback: old-72 group **10 lifted REAL, 5 direct REAL, 5 UNKNOWN**; other parametric group **0 lifted REAL, 3 direct REAL, 17 UNKNOWN**. Per-row outcomes, overrides, arities, stage costs, timeout stage and pinned code/tool hashes are in `informal/`; medians and selection details are in `informal-summary.md`. `informal/results.review-fixed.tsv` corrects the 22 UNKNOWN route labels without changing frozen raw records. These are development observations, not performance results for patch 07, a full-corpus campaign, or a holdout evaluation.

Scope limits: source regimes with unstable origins or roles, unmatched temporal/bus state abstractions, encoded-width changes, interacting index axes, and predicates above the global arity or subset caps decline lifting. The additional sample had no lifted closure, so coverage beyond the demonstrated generated cases and old-72 rows remains an open measurement and algorithm question. This limit cannot create a wrong verdict because no decline or unverified candidate is accepted.
